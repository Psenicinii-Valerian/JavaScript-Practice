// arrow function
// example of definition
// let func = (arg1, arg2, ... , argN) => ExpressionJS; 

let func = function(arg1, arg2){
    return 45*3;
}

let sum = (a,b) => a+b;
console.log(sum(23,7));

let multilineFunction = (x, y, z) => {
    // some code
    // other code
    // to exit us return, example: 
    return true;
}